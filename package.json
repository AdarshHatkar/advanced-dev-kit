{
  "name": "advanced-dev-kit",
  "version": "0.1.0",
  "description": "CLI toolkit for development workflows",
  "bin": {
    "adk": "./dist/cli.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "tsup": "^7.3.0",
    "typescript": "^5.4.0"
  },
  "files": ["dist"]
}
